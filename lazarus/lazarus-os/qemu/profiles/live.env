# Arcology Lazarus OS live-ISO QEMU profile.
MODE=live
ISO=lazarus/lazarus-os/build/arcology-lazarus-live.iso
: "${QEMU_MEMORY:=4096}"
: "${QEMU_CPUS:=2}"
: "${QEMU_DISPLAY:=gtk}"
: "${QEMU_WIDTH:=1024}"
: "${QEMU_HEIGHT:=768}"
: "${QEMU_FIRMWARE:=uefi}"
: "${QEMU_BOOT_MEDIA:=usb}"

#IMPORT "sysadmin"

logPath = "arco-file-log-example.txt"
IF LEN(Args) > 0 THEN logPath = Args[0]
SysAdmin.AppendLog(logPath, "example log entry")
PRINT File.ReadText(logPath) CONTAINS "example log entry"

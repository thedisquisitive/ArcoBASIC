#PROFILE UEFI
#TARGET X86_64
#RUNTIME NONE

FUNCTION Main(database AS MMIOPTR) AS U64
    IF PRDReserveRange(database, 8, 1048576, 2, 1, 100) = 0 THEN RETURN 1
    RETURN MEMORY.Read32(database)
END FUNCTION

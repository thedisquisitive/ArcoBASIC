#PROFILE UEFI
#TARGET X86_64
#RUNTIME NONE

FUNCTION Main(mapBuffer AS MMIOPTR, database AS MMIOPTR) AS U64
    IF PRDTranslateFirmwareMap(database, 64, mapBuffer, 3072, 48) = 0 THEN RETURN 1
    RETURN PRDRegionCount(database)
END FUNCTION
